{
  "name": "@jack-k/gateway",
  "version": "0.1.2",
  "type": "module",
  "dependencies": {
    "@jack-k/core": "workspace:*",
    "@jack-k/config": "workspace:*",
    "@jack-k/telegram": "workspace:*",
    "@jack-k/ticket-store": "workspace:*",
    "hono": "^4.6.14"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. apps/gateway/src",
    "start": "node dist/index.js"
  }
}
